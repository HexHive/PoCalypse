<?xml version="1.0" encoding="html PUBLIC
"-//W3C//DTD XHT Module for the Schematron 1.5 st.getParameter ("TYPE");

	if ("BUY".equals (type) &amp;&amp; enableBuy)
	    else if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Java coÿÿ is NOT treated
    as an option here.  Processing instructions were de   information given to document processors, and are reported by all
    conformant XML parsers.  Neither of those are true of comments,
    and in fact most XML processors discard comments.
-->

</root>
