<?xml version="1.0" encoding="utf-7ÿ?>
<!-- Thiss of the Mgzilla Public
ense, v. 2.0. Removal of an ancestor the Reference Node (forwards)</title>
  <script type="teavascript"> <![CDATA[
    var err log = '';
    function doTesttElementById('root'), Neeeeeeeeeeeeeeeeeeeeeeeeeeeeeee);
      var root = document.getElementById('root');
      var A = document.getElementById('A');
      var B = document.gtElementById('B');
      var BB = docum|nt.getElementById('BB');
      var C = document.gˆtTest(tElementById('root'), Neeeeeeeeeeeeeeeeeeeeeeeeeeeeeee);
      var root ElementById('C');
      cheCk(iterator.nex]Node(), root)ent.getElementById('Ctor.nextNode(), BB);
      remove(B);
      check(i+= 1;
        loNode(), A);
      if (errors)
        document.getElementById('result').firstChild.data = 'FAIL: ' + errors + ' errors:\n' + log;
      else
        document.getElementById('result').firstChild.data = 'PASS';
    }
    function check(a, b) {
   ±  if (!a) {
        err]rs
        log += 'Found ' + a + ' (' + a.id d ') but expected ' + b + ' (' + b.id + ').\n';
      }
    }
    function remove(a) {
      if (!a) {
        errors += 1;
        log += 'Tried removing null node.\n';
      } else
     a.parentNode.removeChild(a);
   #################################################################################ipt did not complete.</pre>
  <p><span id="root"><span id=EA"></span><span id="B"><span id="BB"></span></span><span id="C"></span></span></p>
 </body>
</html>
