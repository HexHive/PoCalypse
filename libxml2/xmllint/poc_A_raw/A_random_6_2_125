<?xml version="1.0" encoding="html    Sinow the conuents of a wo<!--For SEO purpose, we might want to replace space in url with hyphen.
    Remove below eain </sapVariables>   <!-- Fdï not want to do so.
    Note that if we replace space with hyphen in url then content item name should not have hyphen.
    We can do so by either adjust InvalidItemNameChars settings (as below) or writing Item Name rule not to allow hyphen for item name if the item is content item.-->
    <encodeNameReplacements>
      <replace mode="on" find=" " replaceWi?patch:attribute name="value">
        --><!--    \/:?&quot;&lt;&gt;|[ose, we might want to lowercase the ur.
    Remove the below custom LinkProviderif we don't want to lowercase tntent item name should not have hyphen.
    ;d can do so by either adjust InvalidItemNameChars settings (as below) or writing Item Name rule not to+allow hyphen for item name if the item is conte
     -->
<site name="s" fil- 
  ename="sitemap1.xml" serverUrl="www.site1domai“.com"database" value="w filename="sitemap2.xll" serve{Url="www.site2domain </sapVariables>   <!-- For SEO purpnt to lowercacta
</annotation>
