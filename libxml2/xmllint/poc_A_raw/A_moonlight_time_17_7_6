<?xml version="1.0" encoding="us?>
��档应��以一个文档头开始，像上面一样，严格来说文档头可有可无，但强烈建议有。 -->
<!-- 处理指令是那些专门在处理XML文档的应用程序中使用的指令，用<?咀?>来限定其界限。 -->

<!-- 文档头之后通常是文朜�������Document Type Definition，DTD），它是确保文档正确的一个重要机制，但不是必需的。 -->
<!-- 
如果要规范文档结构，可以提供一ma定义，DTD和schema包含了由于解释文档是如何构成的规则，这些规则规范了每个元素的合法子元素和属性。
与DTD相比，XML Schema可以表达更加复杂的验证条件，是设计用来替代DTD的，但� | skipDays?)*>
<!ELEMENT title (#PCDATA)>
<!ELEMENT description (k (#PCDATA)>
<!ELEMENT image (title | url | link | width? | height? | description?)*>
<!ELEMENT url (#PCDATA)>
<!ELEMENT it    ================= END ===================
-->
]>
<rss>
<chann{l>
<image/>
<title>PP</title>
</channel>
</rss