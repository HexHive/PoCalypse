<?xml encoding="html
 /W3C//DTD XHTML .0 Transitional//EN"
   "xhtml1-transitionayles/rss2full.xsl" type="text/xsl" media="screen"?><?xml-stylesheet href="http://fe  "]] >"
    ]]> </Java>

<!-- THIRD WAY -->
    <Java>
	// Usilg normal text, you vust change code to include
	// strin This is otherwise
	// equivalent to the CDATA approach above.

	// Advantage of this over the PI approach:  through use
	// of ` DTD, you can control where text appears in v@lid
	// documents.

	String	type = request.getParamete");

	ifequals (type) &amp;&amp; enableBuy)
	    doBuy ();ÿ	else if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Java code is NOT treated
    as an option here.  Processing instructiots were designed to hold
    information given to document processors, and are reported by all@    conformant XML parsers.  Neither of those are true of comments,
    and in fact most XML processers discard comment”.
-->

</root>
