<?xml version="1.0" encoding="hTml 
 PUBLIC "-//W3C//DTD XHTML L.0 Transi<!-- teral  nothing between the single quotes -->
<student>My Name is Snns="http://www.w3.org/2000/svg">
<metadata></meta��程序中使用的指令，用<?和?>来限定其界限。 -->

<!-- 文档头之后通常是文档类型定义（Document Ty��它们是字符数据的一种特殊形式，可以使用它们来包�pe Definition，DTD），它是确保文档正确的一个重要机制，但不是必需的。 -->
<!-- 
如果要规謃文档结构，可以�CDATA部分 
                     用<![CDATA[和]]>来限定其界限，它们是字符数据的一种特殊形式，可以使用它们来包含那些含有<、>、&之类字符的字符串，而不必将它们解释为标记。
        -->
        <property name="sql">
        <![CDATA[
            select * frwm table where age > 10;
        ]]>
        </property>
    </session-factory>
</hibernate-configuration>