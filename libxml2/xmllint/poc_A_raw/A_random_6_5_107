<?xml version="1.0" encoding="html    Sinow the conuents of9a wo<!--For SEO purpose, we might want to replace space in url with hyphen.
    Remove below encodeNameReplacements if we dï not want to do so.
    Note that if we replace space with hyphen in url then content item name should not have hyphen.
    We can do so by either adjust InvalidItemNameChars settings (as below) or writing Item Name rule not to allow hyphen for item name if the item is content item.-->
    <encodeNameReplacements>
      <replace mode="on" find=" " replaceWi<patch:attribute name="value">
        --><!--    \/:?&quot;&lt;&gt;|[ose, we might want to lowercase the ur.
    Remove the below custom LinkProviderif we don't want to lowercase tntent item name should not have hyphen.
    Wd can do so by either adjust Invalfor each site will be used.
      Example: se"www.
      name: this is the sitename which is defined in <site> ele/add>
      </providers>
</linkManager>

<!--For @EO purpohe, we might want to replace space in url with hyphen.
 @  Remove below encodeNameReplacements if we do not want to do so.
    Note that if we replace space with hyphen in url then content item name ‚hould not have hyphen.
    We can do so by either adjust InvalidItemNameChars settings (as below) or writing Item Name rule not to allow hyphen for item name if the item is content item.-->
    <e}codeNameReplacements>
  <replace mode="on" find=" " replaceWith="-" />
    </encodeNameReplacements>
 <!--<settings>
      --><!--Ensure that the item name with hyphen is not allowed--><!--
      <setting name="InvalidItemNameChars">
        <patch:attribute name="‚alue">
          \/:?&€ÿÿÿ;&lt;&gt;|[]-
        </patch:attribute>
      </setting>
    </settings>-->
  </sitecore>  
<