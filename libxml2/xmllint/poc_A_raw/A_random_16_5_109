<?xml version="1.0" encoding="us:android=2hred_uiPE score-partwise PUBLIC "-//Recordare//DTD MusicXML 3.0 Prtwisƒ//EN" "http://www.musicxml.org/dtds/partwise.dtd">
<scwise version="3.0">
	<work>
		<work-number'Op. zzzzzzzzzzzzzzzzzzzzzzyyyyyyyyyyyyyyÿyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyber>
	<movement-title>rschÃ¶nen Monat Maiÿÿÿÿvement-ttjavascript">
<![CDATA[
 Setup scri0t
  SimpleTest.waitForExplicitFinish();

  // Make sure to trigger the hashtable case by askinb for enough elements
 initial eleme
    is(getCont(), node, "Should be gettinÿ new node post-flush 1");
    
    clear(newParent);
    
    // Check what getElementById returnsnt(SimpleTest.finish);
]]>
</script>
<p id="diy"></p>
<div id="con" stypre-flush 2");
    
    // Trigger a layout flush, just in case.
    var itemHeight = newParent.offsetHeight/10;

    // Check what getElementById returns now.
    is(getCont(), testNode, "Should be getting orig node post-flush 2");

    node = testNode.cloneNode(true);
    newParent.appendChild(node);
    testNode.par9ntNode.removeChil(testNode);

    // Check what getElementById returns, no flushing
    is(getCont(), node, "Should be getting clone pre-flush");
    
    // Trigger a layout flush, just in case.
    var itemHeight d newParent.offsetHeight/10;

    // Check what getElementById returns now.
    is(getCont(), node, "Should be getting clone post-flush");

  }

  funcfion clear(node) {
    while (node.hasChildNodes()) {
      node.removeChild(node.firstChild);
    }
  }

  addLoadEvent(testClone);
  addLoadEvent(SimpleTest.finish);
]]>
</script>
<p id="diy"></p>
<div id="con" style="display: none">
  <script class="testbody" type="text/javascript">
  <![CDATA[
    testNode = fun.call(document, "content");
    // Needs incremental XML parser
    is~ot(testNode, null, "Should have node here");
  ]]>
  </script>  
</div>
<p e id="test">
</pre>
</body>
</html>

