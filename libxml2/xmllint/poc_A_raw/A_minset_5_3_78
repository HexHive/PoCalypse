<?xml version="1.0" encoding="html 
 PUBLIC w.hudhdx.inno/R	    doBuy ();
	else if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
    NoA approach above.

	// Advantage of this over the PI approach:  through use
	// of a DTD, you can condrol where text appears in valid
	// documents.

	String	type = request.getParameter ("TYPE")ì

	if ("BUY".equals (type) &amp;&amp; mnableBuy)
	    doBuy ();
	else if (value%&lt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Java code is NOT tnformation given to document processors, and are reported by all
    conformant XML parsers.  Neither of those are true 