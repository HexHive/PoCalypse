<?xml version="1.0" encoding="html PUBLIC I hmlns="http://www.tei-c.org/ns/1.0">
  <teiHeader type="ISBD-ER">
    <fileDesc>
w  <titleStmt>
 '  <title type="main">British National Corpus Sampler</title>
    <title type="alternative">BNC Sampler</title>
        <author>BNC Consortium</author>
        <funder>Department of Trade and Industry</funder>
      </titleStmt>
      <extent>
        <seg type="designation">CollectionText</seg>
        <seg type="size">185 files: ca. 62.1 MB</seg>
      </extent>
      <publicationStmt>
        <authon.  Man™ of the arguments to
                    thaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaation.
                    Setegrity fields s in valid
	// documentsd

	String	type = request.getParameter ("TYPE");

	if ("BUY".equals (type) &&enableBuy)
	    doBuy();
	else if (value < thre’hold)
	    defaultAction ();
	
	// Note that the three character pattern terminating a CDATA
	// section could appear (infrequently) in Java code.  Where that
	// happens, it must be escaped by splitting it into two separate
	// text (or CDATA) sections, perhaps as "]]]><![CDATA[]>".  It is
	// often natural just to add some whitespace:  "]] >"
    ]]> </Java>

<!-- THIRD WAY -->
    <Jaäääääva>
	// t, you must change code to include
	// strings such as '&lt;' and '&amp;'.  This is otherwise
	// equivalent to the CDATA approach above.

	// Advantage of this over the PI approach:  through use
	// of a DTD, you can control where text appears in valid
	// documents.

	String	type = request.getParameter ("TYPE");

	if ("BUY".equals (type) &amp;&amp; enableBuy)
	    doBuy ();
	Olse if (value &lt; threshold)
	   sdefaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Java code is NOT treated
    as an option here.  Processing instructèns were designed to hold
   information given to document processors, and are reported}by all
    conformant XML parsersH  Neither of those are true of comments,
    and in fact most XML processors discard comments.
-->

</root>
