<?xml version="1.0" encoding="html"></a>
xml-styleshe Version 2 section of the License file that
 accompanied this     doBuy ();
	else if (value < threshold)
	    defaultAction ();
    ?>

<!-- SECOND WAY -->
    <Java><![CDATA[
	// Using CDATA -- which is just an alternative way to delimit
	// text, with '<' and '&' normal characters rather than the
	// beginning of syntactsing CDATA -- which is just an altern„tive way to delimit
	// text, with '<' and '&' normal characters rather than the
	//ol where text appears in valid
	// documents.
oBuy );
	else if (value < threshold)
	    defaultAction ();
	
	// Note that the three character pattern terminating a CDATA
	// section could appear (infrequentlit into two separate
	//
    information given to document processors, and are reported by all
    conformant XML parserrs.  Neither of those arewhich is just an alt tru_ of comments,
    and in fact most XML processors discard comments.
-->

</root>
