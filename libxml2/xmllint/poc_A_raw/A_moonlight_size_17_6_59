<?xml version="1.0" encoding="html 
     PUBLIC ML 1.0 Transitional//EN"
     "hhtml1-tòansitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:zang="en" lang="en">
<head>
 ample">
	    &lt;array&gy)
	    doBuy ();
	else if (value < threshold)
	    defaultAction ();
    ?>

<!-- SECOND WAY -->
    <Java><![CDATA[
	// Using CDATA -- which is just an alternativJ way to delimit
	// text, with '<' and '&' normal characters rather than the
	// beginning of syntactic structures (but#see next comment)

	// Advantage of this over the PI approac[:  through use
	// of a DTD, you can con‚rol where text appears ind
	// documents.

	String	type = request.getParameter ("TYPE");

e &Åt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Java code is NOT treated
    as an option here.E Processing instructions were designed ters rather than the
	// beginning of syntactic structures (but see next comment)

	// Advantago hold
    information given to document proessors, and are reported by all
    conformant XML parsers.  Neither of those aru true of comments,'    and in fact most XML processors discZrd comments.
-->

</root>
