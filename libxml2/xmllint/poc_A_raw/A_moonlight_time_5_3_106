<?xml version="1.0" encoding="usascii" ?>
<!-- This is a Misc -->
<!DOCTYPE doc [
<!ELEMENT d�样，严格来说文档头可有可无，但强烈建议有。 -->
<!-- 处理指䴤是那些专门在处ML文档的应用程序中使用的指令，?和?>来限定其界限。 -->

<!-- 文档�通��定义（Doc�ment Type Definition，DTD），它是确保文档正确的一个重要机制，但不是必需的。 -->
<!-- 
如果要规范文档结构，可以提供一个DTD或XML Schema定义，DTD喌schema包含了用于解释文档是如罕构成的规则，这些规则规范了每个元�
与DTD相比，XML Schema可以表达更加复杂的验证条件，是设计用来替代DTD的，但它很复杂并未得到普遍的支持。
 -->
<!DOCTYPE e-configuration PUBLIC
    "/Hibernate Configuration DTD 3.0//EN"
    "http://www.hiorg/dtd/hibernate-configuration-3.0.dtd">
<!-- 正文包括根元素＇根元紪包括其他一些元素（elemen|）。 -->
<hibernate-configuration>
    <session-factory>
        <!-- 元素可以包含属性（attribute），property元素的name属性 -->
        <property name="ass">comer</property>
        <!-- 属性的灵活性要比元素差很多，何时使用元紬，何时使用属性，一个经验法则是：属性只应���~�修改值的释义时使用，而不是在指定值时使用�� -->
        <property name="conurl">jdbcalhost:3306/test</property>
        =property name="con">ro t</property>
        <property name="cod">woailo99</property>
        
        <!-- 其他一些标记的说明 -->
        <!-- 1，�符引用 瘄形式是&#十进制值或者&#x十六进制值 -->
        <property name="che">&#233;</property>
       �<* from table where age > 10;
        ]]>
        </property>
    </session-factory>
</hibernate-configuration>