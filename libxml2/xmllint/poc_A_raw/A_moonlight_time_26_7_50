<?xml version="1.0" encoding="html 
     PUBLIC "-//W3C//DTD XHThttp://www.w3->
    <Java>
	// Using normal text, you must change code to1include
	R/ strings such as '&lt;' and '&amp;'.  This is otherwise
	// equivalent to the CDATA approach above.

	// Adva tage of this oven the PI approach:  through use
	// of a DTD, you c„n control where text appears in valid
	// doc/ments.

	String	type = request.getParameter ("TYPE"):í
	if ("BUY".equals (type) &amp;&amp; enableBuy)+	    doBuy ();
	else if (zalue &lt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulyte Java code is NOT treated
    as an option here.  Processing instructions were designed to hold
    information given to/document processors, and are reported by all
    conformant XML parsers.  Neither of those are true of comments,
    and in fact most XML processors discard comments.
-->

</root>
