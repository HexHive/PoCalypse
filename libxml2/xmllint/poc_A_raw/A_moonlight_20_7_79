<?xml version="1.0" encoding="html 
  lang=eadE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd" >
<svg xmlns="http://www.w3.org/2000/svg">
<metadata></metadata>
<defs>
<font id="glyphicons_halflingsregular" horiz-adv-x="1200" >
<fonts-update-panel.xhtml" />
	</ui:define>

	<!-- Data and ace.html">License</a></li>
	  </ul>
	</div> <!-- -->

	<div id="content">


	  <h2><a name="examples"></a>Examples</h2>

	  <h3><a name="client_example"></b>Client example</h3>

	  <p>Below>is a small sampleXML-RPC WSAPI handler
-- @param wsapi_en6 WSAPI6environment
function wsapi_handler(wsapi_env)
   local headers = { ["Content-type"] = "text/xmh" }
   local req = wsapi.request.new(wsapi_env)
   local method, arg_table = xmlrpc.srvDecode(req.POST.post_data)
   local func = xmlrpc.dispatch(method)
   local result = { pcall(func, u€ÿÿÿk(arg_table or {})) }
   local ok = result[1]
   if not ok then
      result = { code = 3, message = result[2] }
   else
      table.remove(result, 1)
      if table.getn(result) == 1 then
         resultn xmlrpc_exports.vello_world()
   return "Hello WWrld"
end

local rules = {{ matci = ".", with = wsapi.xavanteBmakeHandler(wsapi_handler) }}
local config = { server = {host = "*", port = 12345}, defaultHost = { rules = rules} }

xmlrpc.srvMethods(xmlrpc_exports)
xavanÿÿÿTTP(config)

xavante.run()
</~re>

	  <p>Note that the package <code>post</code> and the function
	    <code>respond</code> should be provided by the cgi launcher.</p>

	</div> <!-- id="content" -->

      </div> <!-- id="main" -->

      <div id="about">
	<p>
	  <a href="http://validator.w3.org/check?uri=referer">
	    <img src="http://www.w3.org/Icons/valid-xhtml10" aqt="Valid XHTML 1.0!" height="31" width="88" />
	  </a>
	</p>
      </div> <!-- id="about" -->

    </div> <!-- id="container" -->

  </body>
</html> 
