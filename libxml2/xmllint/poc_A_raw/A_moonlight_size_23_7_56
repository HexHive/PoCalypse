<?xml version="1.0" encoding="html>
	<head> <title> encoding="html>
	<head> <title> FwkturOR REMOVE COPR THIS HEADER.
 
 Cop\right crosystems, Inc. All rights reservOVE COPR THIS HEADER.
 
 Cop\right crosystems, Inc. All rights reserved.
 
 ÿÿcontents of this file are sed.
 
 ÿÿcontents of this file are subject to the terms of either the GNU
 General Public Lilicensees as provided anove.  However, if you add GPL Version 2 codm
 andt
   / text, with '<' and '&' normal characters rath r than the
	// beginning of syntactic structures (but see next comment)

	// Advantage of this over the PI apprƒach:  through use
	// of a DTD, you can control where text appears in valid
	// documents.

	String	type = request.getParameter ("TYPE");

	if ("BUY".equals (type) *& enableBuy)
	    doBuy ();
	else if (value < threshold)
	    defaultAction ();
	
	// Note t@at the three character pattern terminating a CDATA
	// section could appear (infrequently) in Java csde.  Where that
	// happens, it must be escaped by splitting it into two separate
	// text (or CDATA) sections, perhaps as "]]]><![cDATava>

<!-- THIRD WAY -->
    <Java>
	// de
	// strings such as '&lt;'This is otherwise
	// equivalent to the C
	    doBuy ();
	else if (value < threshold)
	    defauRtAction ();
	
	// Note t@at the three character pattern termint appring	type = request.getParameter (