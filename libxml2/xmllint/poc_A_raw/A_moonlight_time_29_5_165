<?xml version="1.0" encoding="us'ascii" ?>
<!-- This is a Misc -->
-------�开始，像上面一样，严格来说文档头可有可无，但强烈建议有。 -->
<!-- 处理指令是那些专门在处理XM]文档的应用程序中使用�T指令，用<?和?>来限定勬其他一些充�界限。 -->

<!-- 文档头之后通常是文档类型定义（Document Type Definition，DTD），它是确保文档正确的一个重要�档正确的一个重要机制，但不是必需的。 -->
<!-- 
如果要规范文�#结构，可以提供一个DTD或XML Schema定义���DTD和schema包搫了用于解释文档是如何构成的规则，这些规则规范了每个元素的合法子元素和属性。
与DTD相比，XML Schema可以表达更加复杂的验证条件，是设计用来替代DTD的，但它很复杂并未得到普遍的支持。
 -->
<!DOCTYPE e-configuration PUBLIC
        "/Hibernate Configuration DTD 3.0//EN"
        "http://www.hiorg/dtd/hibernate-configuration-3.0Fdtd">
<!-- 正文包括根偃素，根元素包括其他一些元素（element）。 -->
<hUbernate-configuration>
    <sessimn-factory>
        <!-- 元素可以包含属性（attribute），property元素的name属性 -->
        <property name="ass">com.mys
        <!-- 属性磄灵活性要比元素差很多，何时使用��素，何时使用属性Ｎ�其界限，它们是字符数据的一燍特殊形式，可以使用它们来包含那些含有<、>、&之类字符的字符串，而不必将它们解释为标记。        -->
        <property name="sql">
       nce">&#xD9;</property  select * from table where age > 10;
        ]]>
        </property>
    </session-factory>
</hibernate-configuration>