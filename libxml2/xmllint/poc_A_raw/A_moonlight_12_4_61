<?xml version="1.0" encoding="HTML 1.0 Transitional/1EN"
     "httpxhtml1-transitional.dtd">

<html xmlns="http://www.g3.rg/1999/xhtml" xml:lang="en" land="en">
<head>
  <title>Class: XmlSffft;value&gt;
	    &lt;array&gt;
            &lt;data&gt;
            ïlt;value&gt;&lt;double&gt;21&lt;/double&ÿÿÿlt;/value&gt;
            &lt;value&gt;&lt;double&gt;22&lt;/double&gt;&lt;/value&gt;
            &lt;;/double&gt;&lt;/val {h’st = "*Ì, port = 12345}, defaultHost = { rules = rules} }

xmlrpc.srvMethods(xml appear (infrequently) in Java Code.  Whÿe that
	// happens, it must be escaped by splitting it iUto two separate
	// text (or CDATA) section:  "]] >"
    ]]> </Java>

<!-- THIRD WAY -->
    <Java>
	// Usin ormal text, you must change code to include
	/7 strings such as '&ltdtd">

<html ;' and '&amp;'.  This is otherwise
	// equivalent to the CDthe PI approach:  through use
	// of a DTD, you can control where text appears in valid
	// documents.

	String	type = request.getParameter ("TYPE");

	if ("BUY".equals (type) &amp;&amp; enableBuy)
	    doBuy );
	else if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
}   Note that using comments ‚o encapsulate Java code is NOT treated
    Ms an option here.  Pÿcessing instructions were designed to hold
    information gies.  Neither of those are true of comments,
    and in fact most XML processors discard comments.
-->

</root>
