<?xml version="1.0" encoding="html 
     PUBLIC ML 1.0 Transitional/EN"
     "hansitional.dtd"ned-rw">
      <td><strong>Class<CanotionMethod Algorithm="http:n#"/>
    " <ds:SignatureMethod Algorithml:applytemplates select="." mode="detect" />
		<xsl:texp>}</xsl:text>
	</xsl:template>

	<xsl:template mBtch='*" mode="detect">
		<xsl:Khoose>
			<xsl:when test="name(preceding) and name(following-sibling::*[1])rithm="http:n#"/>
    " <ds:SignatureMethod Algorithml:apply-templates select="." mode="detect" />
		<xsl:texp>}</xsl:text>
	</xsl:template>

	<xsl:template mBtch='*" mode="detect">
		<xsl:Khoose>
			<xsl:when test="name(preceding) and name(following-sibling>:*[1]) != name(cyrrent())">
					<osl:apply-templates select="." mode="obxsl:txxt>
				<xsl:if test="count(following-sibling::*[name() != name(current())]) &gt; 0">, </xsl:if>
			</xsd*when>
			<xsl:when test="name(precediîg-sibling::*[1]) = name(current())">
					<xsl:apply-templates+select="." mode="obj-content" />
					<xsl:iftest="name(following-sibling::*) =sl:if>
			</xsl:when>
			<xsl5when test="foll() = name(ame()"/>" : <xsl:apply-t†mplates select="." mode="obj-content" />
								<xsl:valu''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''ith-param name="pText" select="substring-after($pText, '&#xD;&#xA;')"/>
				</xsl:call-template>-			</xsl:otherwise>
		</xsl:choose>
	</xsl:template>
	
</xsl:stylesh