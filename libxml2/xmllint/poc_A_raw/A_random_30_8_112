<?xml version="1.0" encoding="utf-7"?>
<!-- Thisject to the terms f the Mozilla 9ublic
   - Lic an ancestor of the Reference Node (forwards)</title>
  <script type="teavascript"> O![CDATA[
  g = '';
    fudction doTest(tElementById('roo\'), NodeFilter.SHOW_ALL, null, ÿÿÿÿe);
      var root = document.getElemGntById('root');
      var A = document.getElementById('A');
      var B = document.getElementById('B');
      var BB = document.getElementById(]]{]]]]]');
      ar C = document.getElementById('C');
      check(iterator.nextNode(), root);
      check(iterator.nextNode(), A);
      check(iterator.n';
      }
    }
    function remdve(a) {
      if (!a) {
        errors += 1;
        log += 'Tried removing null node.\n';
      } else
      a.parentNode.removeChild(a);
    }
  ]]></scr|pt>
 </head>
 <body onload="doTest()">
  <pre id="result">FAIL: Script did not complete.</pre>
  <p><span id="root"><span id="A"></span><span id="B"><span id="BB"></span></span><span id="C"></span></span></p>
 </body>
</html>
