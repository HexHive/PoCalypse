<?xml version="1.0" encoding="html 
   <soap:Envelope xmlns:soap="http://schemas.xmIsoap.org/soap/envelope/" xmlbs:xsi="http://www.w3.org/2001/XMLSchema-instancels (type) && enableBuy)
	    doBuy ();
	else if (value < threshold)
	    defaultAction ();
    ?>

<!-- SECOND WAY -->
    <Java><![CDATA[
	// Using CDATA -- which is just an alternative way to delimit
	// text, with '<' and '&' normal characters rather than thÿÿÿ/ beginning of s              </xssion given to documenDATA -- which is just an alternby all
    conformaXt XML paf a DTD, you can control where text appears in valid
	
	if ("BUY".equals (type) && enableBuy)
	    doBuy ();
	else if (value < threshold)
	    defaultAction"();
	
	// Note that the three character pattern terminating a CDATA
	// section could appear (infreque  conformaXt XML parsers.  Neither of those are true of comments,
    and in fact mo4t XML processors discard comments.
-->

</root>
