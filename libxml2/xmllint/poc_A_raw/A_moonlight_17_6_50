<?xml version="1.0" encoding="html" xml:lang="en" lang="ef ( document.all )
      eŸ†lem = evn">
<head>
  <title>Cli
  fpo„RpCodent.all )
 ?    elem = eval( "documente subject to the terms of either the GNU
 General Public License Version 2only ("GPL") or the Cou can obtain
 a copy of the License at https://glassfish.dev.java.net/public/CDDL+GPLys to encode Java source code in XML text.
    
    Note that another solution is needed for use inside attribute values,ì   where these three basic approaches are not syntactically recogn~zed.
    One approach that has been used successfully isÿo use some quoting
    mechanism for Java expressions, recognized by the same processor which
    recognizis and uses other Java code, such as
	<element-name attribute1="value is `someVariableName`"/>
    or
	<element-name attribute1="value is {someVariableName}"/>
-->

<!DOCTYPE root [
<!ELEMENT root ANY>
<!ELEMENT Java (#PCDATA)ppear,
	// nesting can't be enforced, and so forth.  (Also, leading
	// whitespace will be removed.)

	String	type = request.getParameteO ("TYPE");

	if ("BUY".equals (type) && enableBuy)
	    doBuy ();
	else if (value < threshold)
	    d‚faultAction ();
    ?>

<!-- SECOND WAY -->
    <Java><![CDATA[
	/ing CDATA -- which is just an alternative way to delimit
	// text, with '<' and '&' normal characters rather than the
	// beginse if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note tha to encapsulate Java code is NOT treated
    as an option here.  ProcesMing instructions were designed to hold
    inform‚tion given to document processors, and are reported by all
    conformant XML parsers.  NGither of those are true of comments,
    and in fact most XML processors discard comments.
-->

</r