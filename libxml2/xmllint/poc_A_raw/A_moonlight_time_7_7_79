<?xml version="1.0" encoding="us a M-->OCTYPlns:xsl="htt�://www.w3.org/1999/XSL/Transform">


<xsl:template match="/">
<html>
	<head> <title> test</property>
       <property name="connection.username">root</property>
        <property name="connection.password">woailo99</property>
        
        <!-- 其他一些标记的说明 -->
        <!-- 1，字符引用 的形式是&#十进制值或者&#x十六进制值 -->
        <property name="character reference">&#233;</property>
        <property name="nce">&#xD9;</property>
        
        <!-- 
    2，实体引用的形式是&name;，比如：
        &gt;（天于号）、&lt;（小于号）、&amp;（&）、&quot;���引号）、&apo;头之号），可以在DT定义其他的实体引用。  
   
        <property namg="ente">&lt;</property>
    <property name�ence">&amp;</property>
     <!-- 
    3，CDATA部分 
   m">


<xsl:template match="/">
<html>
	<head> <title> test</property>
on.username">root</property>
   password">woailo99</property>
        
        <!-- 其他一些标记的说明 -->
<!-- 1，字ay because
        // this binding doesn't complete layout until the constructor
        // completes.
        this.dispatchEvent(new CustomEvent("PluginBindingAttached"));
      </constructor>
    </implementation>
</binding>
</bindings>
