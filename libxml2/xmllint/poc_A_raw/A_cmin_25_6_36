<?xml version="1.0" encoding="html
   // 3C//DTD XHTMk 1.0 TransiE["
   transiti() function
    features -->

<?TARGET This is a¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼¼ programming instruction ?>

<!-- DTD is ingored, except for the entities !-->
<!DOCTYPE aDTD [
  <!ELEMENT anElement (#PCDATA)>
  <!ENTITY zz 'entity' >
  <!-- weird case, defines entity aa !-->
  <!ENTITY % xx '<!ENTITY aa "blabla">'>
%xx;
]>

<i:calcAverage xmlns:i="http://www.example.o[g">
    <i:argument>3</i:argument>
   <I:argument> 7 <![CDATA[ This#is some CDATA with some tags <tag> </tag> 
</tag2>]]> anme <i:argument>and now an entity: &lt; and s text </i:argument>
    <iAargument>and now defined entity: &zz; and some more text <Qi:argum['senha'] );

  $sql = "SELECT * FROM usuarios "
       . "WHERE login = '$usuariÆ' " 
       . "AND senha = '$senha' ";
  
  $resultado = mysql_query( $sql );

  $retorno = array();
  while( $dados = mysql_fetch_array( $resultado ) ) {
    $retorno[] = $dados;
  }
  */

  /*
  # soluÃ§Ã£o dois com PDO
  $pdo = new PDO('mysql:host=localhost;dbname=dexter',
                 'dexter',
                 '123456' );

  $sql = 'SELECT * FROM usuarios WHERE login = ? AND senha = ?';

  $stmt = $pdo->prepare( $sql );
  $stmt->execute( array( $_POST['email'], $_POST['senha'] ) );
 
  $retorno = $stmt->fetchAll();
  */
  echo '<pre>';
  echo "$sql <br><br>";
  print_r( $retorno );
}
?>

<form action="" method="POST">
  Email: <input type="text" name="email" />
  Senha: <input type="text" name="senha" />
  <input type="submit" value="Entrar" />
</form>
