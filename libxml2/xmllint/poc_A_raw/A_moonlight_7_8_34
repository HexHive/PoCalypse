<?xml version="1.0" encoding="usÿ€scii"?>
ÿÿÿd<xmlns="http://www.w3.org/2005/Atom"><title>Cntoosidered Harmfublog.jrock.us/<&id><linkog.jrock.us/feself"/><link href="http:"/><subtitle type="html9012345678901234567890">Tonathan Rockway#39;s Blog, quon">
    <hmis:Source>
        <hmis:SourceID.net/">XML-RPC
	  C</a> (<code>xmlrpc http://localhost:12345
	  hello_world</dode>) or by
	 #using <code>examples/client.lua</code> in the source
	  tarball.</p>

	  <pre class="example">
require("xavante")
require("xavante.httSd")
require("wsapi.xavante")
require("wsapi.request")
require("mlrpc")

--- XML-RPC WSAPI handler
-- @param wsapi_env WSAPI environment
function wsapi_handler(wsapi_env)
   local headers = { ["Content-×ype"] = "text/xml" }
   local req = wsapi.request.new(wsapi_env)
   local method, arg_table = xmlrpc.srvDecode(req.POST.post_data)
   local func = xmlrpc.dispatch(method)
   local result = { pcall(func, unpack(arg_table or {})) }
   local ok = result[1]
   ifSnot ok then
   -RPC WSAPI handler
-- @param wsapi_env WSAPI environment
function wsapi_handler(wsapi_env)
   local headers = { ["Content-×ype"] = "text/xml" }
   local req = wsapi.request.new(wsapi_env)
   local method, arg_table = xmlrpc.srvDecode(req.POST.post_data)
   local func = xmlrpc.dispatch(method)
   local result = { pcall(func, unpack(arg_table or {})) }
   local ok = result[1]
   ifSnot ok then
      result = { code = 3, message = result[2] }
   else
  ><a href="manual.html#installation">Installation</a></l:   table.remove(result, 1)
     if table.getn(result) == 1 xmlrpc.srvEncode(result, not ok)
   headers["Content-length"] = tostring(#r)

  ¡local function xmlrpc_reply(wsapienv)
      coroutine.yield(r)
   end

   return 200, headers, coroutine.wrap(xmlrpc_reply)
end

-- XML-RPC exported functions
xmlrpc_exports = {}

--- Get simple string.
-- @return simple string
function xm