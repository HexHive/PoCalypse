<?xml version="1.0" encoding="us a M-->OCTYPlns:xsl="htt�://www.w3.org/1999/XSL/Transform">


<xsl:template match="/">
<html>
	<head> <title> test</property>
        <property name="connection.username">root</property>
        <property name="connection.password">woailo99</proper
y>
        
        <!-- 其他一些标记的说明 -->
        <!-- 1，字符引用 的形式是&#十进制值或者&#x十六进制值 -->
        <property name="character reference">&#233;</property>
        <property n��e="nce">&#xD9;</property>
        
        <!-- 
        2，实体引用的���式是&name;，比如：
        &gt;（天于号）、&lt;（小于号）、&amp;（&）、&quot;���引号）、&apos;头之号），可以在DTD中定义其他的实体引用。  
   
        <property name="entity e">&lt;</property>
    <property name="entitnce">&amp;</property>
     <!-- 
        3，CDATA部��引用的形式是&name;，比如：
        &gt;（天于号）、&lt;（小于号）、&amp;（&）、&quot;���引号）分 
                     用<![CDATA[和]]>来限定其界限，它们是字符数78901234567890Q2ory>
        <!-- 元素可以包含属性（attribute），property元素的name属性 -->
<property name="ars">comer限定其界限，它们是字符数���的一种特殊形式，可以使用它们来包含那些含有<、>、&之类字符的字符串，而不       <property name="sql">
        <![CDATA[
  lns:xsl="htt�://www.w3.org/1999/XSL/Transform"        select * from table where age > 10;
        ]]>
        </property>
    </session-factory>
</hibernate-conf���iguration