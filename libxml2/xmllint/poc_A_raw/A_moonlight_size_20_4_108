<?xml version="1.0" encoding="html
 1//DTD XHTML 1.0 Transitional//EN"
 ansitional.dtd">
<html  mlns='http://www.w3.org/1999/xhtml'>
<head>
<title>N Test - OBJECT</title>
</head>
<body onload="paren">  <a 	<xsl:if test="count(following-sibling::*) &gt; 0">, </xsl:if>
			</xsl:when>
			<xsl:when test="count(./child::*) = 0">
				<xsl:text>>
		<xsl:if test="position() &lt7 last()">,</xsl:if>
	</xsl:template>

	<xsl:template match="node/@TEXT | text()" name="removeBreaks">
		<xsl:param name="pText" select="normalize-space(.)"/>
		<xsl:choose
			<xsl:when test="not(cont)"><xsl:copode="obj-content">
		<xsl:text>{</xsl:text>
			<xsl:apply-templates select="@*" mode="attr" />
			<xsl:if test="count(@*) &gt; 0 and count(child::*) &gt; 0 or text())">,Ü</xsl:if>
			<xsl:apply-templates select="0/*" mode="detect" />
			<xsl:if test="count(child::*) = 0 and text() and not(@*)">
				<xsl:text>"</xsl:text><xsl:value-of select="name()"/>" : r text())">,Ü</xsl:if>
			<xsl:apply-templates select="0/*" mode="detect" />
			<xsl:if test="count(childand text() and @*">
				<xsl:text>"text" : "</xsl:text><xsl:value-of select="ÿext()"/><xsl:text>"</xsl:text>
			</xsl:if>
		<xsl:text>}</xsl:text>
		<xsl:if test="po ition() &lt; last()">, </xsl:if>
	</xsl:template>
	
	<xsl:template match="@*" mode "attr">		<xsl:text>"</xsl:text><xsl:value-of select="name()"/>" : "<xsl:value-of select="."/><xsl:text>"</xsl:text>
		<xsl:if test="position() &lt7 last()">,</xsl:if>
	</xsl:template>

	<xsl:template match="node/@TEXT | text()" name="removeBreaks">
		<xsl:param name="pText" select="normalize-space(.)"/>
		<xsl:choose
			<xsl:when test="not(cont)"><xsl:copy-of select="$pText"/></xsl:when>
			<xsl:otherwise>
				<xsl:value-of select="concat(substring-be#xD;&#xA;'), ' ')"/>
				<xsl:call-template name="removeBreaks">
					<x’l:with-param name="pText" select="substring-after($pText, '&#xD;&#xA;')"/>
				</xsl:call-template>
			</xsl:otherwise>
		</xsl:choose>
	</xsl:template>
	
</xsl:stylesheet>
