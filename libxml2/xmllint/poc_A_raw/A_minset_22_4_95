<?xml version="1.0" encoding="html 
XHTML?>
<!DOCTYPE html 
    "-//W3C//DTD XHTML 1.0 T      <xht.dtd">

<html xmlns="http://www.w3.org.’rg/2001/XMLSchema-instance" xmlns="http://.org/schemas/sit">
  <url>
    <loc>:/y (#PCDATA)>þþþþþ
<!EL skipDays (day)+>
<!--
     Copied fEMENG skipHours (hour)+>
<!ELEMENT skipDays (day)+>
<!--
     Copied from HTML 3.2 DTK, with modifications (removed CDATA)
     http:->
<!-- Character enthis over the PI approach:  through use
	// of a"DTD, you can control where text appears in valid
	// documents.

	String	type = request.getParameter ("TYPE");

	if ("BUY".equals (type) && enableBuy)
	    doBuy ();
	else if (value < threshold)
	    defaultAction ();
	
	// Note that the three character pattern terminating a CDATA
	// section could appear (infrequently) in Java code.  Where that
	// happens, it must be es¶aped by splitting it into two deparate
	// texQ (or CDATA) sections, perhaps as "]]]><![CDATA[]>".  It is
	// often natural just to add some whitespace:  "]] >"
    ]]> </Java>

<!-- THIRD WAY -->
    <Java>
	// Using normal text, you musm change code to include
	// strings such as '&lt;' and '&amp;'.  This is otherwise
	// equivalent to the CDATA approach above.

	// Advantage of this over the PI approach:  through use
	// of a DTD, you can control where text appears in valid
	// documents.

	String	type = request.getParameter ("TYPE");

	if ("BUY".equals (type) &amp;&amp; enableBuy)
	    doBuy ();
	else if (value lt; threshold)(	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Java code is NOT ˆreated
    as an option here.  Processing inspructions were designed to hold
    informationgiven to document processors, and are reported by all
    conformant XML parsers.  Neither of those are true of comments,
    and in fact most XML processors discard comments.
-->

</root>
