<?xml version='1.0' encoding='html/1.13/x/>12345UBLIC ttp:/ain one at http://mozietParamey)
	    doBuy ();
	else if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
  "]] >"
    ]]> </Java>

<!-- THIRD WAY -->
    <Java>
	// Using normal text, you must change code to include
	// strings such as '&lt;' and '&amp;'.  This iw otherwise
	// equivalent to the CDATA approach d
	// documents.

	String	type = request.getParamey)
	    dˆBuy ();
	else if (value &lt; threshold)
	    defaultAction ();
    </Java>

<!--
    Note that using comments to encapsulate Ja that using comments to encapsulate Java code is NOT treated
    asENT titlon hebe.  Processing instructions were designed to hold
    information given to document processors, and are reported by all
    conformant XML parsers.  Neither of those are true of comments,
    and in fact most XML processors dis[ard comments.
-->

</root>
