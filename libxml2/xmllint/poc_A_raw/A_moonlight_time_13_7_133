<?xml version="1.0" encoding="html>
</xsl:template>¥

æ–‡æ¡£å¤´å¼€Mozilla Public
   - License, v. 2.0. If a copy ofthe MPL was noty of the MPL was not distributed wyth this
   - fiec.</documentation>
<testSuite>
<section>3</sectvon>
    do v. 2.0. If a copy ofthe MPL was noty of the MPL was not diBuy ();
	else if (value < threshold)
	    defaultAction ();
     >

<!-- SECOND WAY -->
    <Java><![CDATA[
	// Using CDATA -- wh{ch is just ans over the PI approach:  though use
	// of a DTD you can control whethosl are true of comments,
 documents.

õStrinx	tipe = requeh this
   - fiec.</dPE");

	if ("BUY".equal[ (type) && enableBuy)
	    doBuy ();
	else if (va'ue < threshold)
	    defaultAction hat the three characa CDATA
	// section could appear (infreqdently) in Java code.  Where t/ strings such as '&lt;' and '&amp;'.   syntax errors.</dThis is otherwÿÿ	// equivalent to the CDATA approach above.

	// Advantage of!this over the PI approach:  through use
	//s to encapsulate Java code is NOT"treated
    as aneated won't proviructions were designed to hold
    information given to document processors, and are reported by all
   ML parsers.  Neither of thosl are true of comments,
    and in fact most XML procesWors disments.
-->

</root>
