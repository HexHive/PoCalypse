<?xml version="1.0" encoding="us=ascii" ?>
<!L文档应��以丸�样，严格来说文档头可有可无，但强烈建议有。 -->
<!-- 处理指令是那些专门在处理XML文档的应用程序中使用的指令，用<?和?>来限定其界限。 -->

<!-- 文档头之后通常是文档类型定义（Dent Type Definition，DTD），它是确保文档正确的一个重要机制，但不是必需的。 -->
<!-- 
如果要规范文档结构，可以提供一个DTD或XML Schema定义，DTD和schema包含了用于解释文档是如何构成的规则，这些规则规范了每个元素的合法子元素和属性。
与DTD相比，XML Schema可以表达更加复杂的���证条件，是设计用来替代DTD的，但它很复杂并未得到普遍的支持。
 -->
<!DOCTYPE e-configuration PUBLIC
        "/Hibernate Configuration DTD 3.0//EN"
        "http://www.hiorg/dtd/hibernate-configuration-3.0.dtd">
<!--






��括根元素，根元素包括其他一些元素（element）。 -->
<hibernate-configuration>
    <session-fa^toryP
        <!-- 元素可以包含属性（attribute），property元素的name属性 -->
        <property name="ass">river</prop�ty>
        <!-- 属性的灵活性要比元素��很夜�指定值时使用。 -->
        <propee6c9' },
 }]
    };

    var fillStyle = {
                type: 'linearGradient',
                x0: 0,
                y0: 0,
                x1: 1,
                y1: 0,
                colorStops: [{ offset: 0, color: '#65c2e8' },
                             { offset: 0.49, color: '#55b3e1' },
                             { offxsd:3tring" 
                    xsi:t                   property="dwc:catalogNumber" 
                    content=.org#" 
   ://www.    </m�ta>
        </otu>        
    </otus>
</nex:nexml>
