<?xml version="1.0" encoding="us,ascii" ?>coding="us-ascii" ?>
<!-
<!Ddoc EM
<!-- Thi�上面一样，严格来说文档头可有可无，但强烈建议有。 -->
<!-- 处理指令是那些专门在偄理XML文档的应用程序中使用的指令，用<?和?>来限定其界限。 -->

<!-- 文档头之后通常是文档类型定义��Document Type Definition，DTD），它是确保文档正确的一个重要机制，但不是必需的。 -->
<!-- 
如果要规范文档结构，可以提供一个DTD或XML Schema定义，DTD和schema包含了用于解释文档是如何构成的规则，这些规则规范了每个元素的合法子元素和属性。
与DTD相比，XML Schema可以表达更加复杂的验证条件，是设计的，但它--------------------------------�持。
 -->
<!DOCTYPE e-configuration PUBLIC
        "/Hibernate Configuration DTD 3.0//EN"
        "http://wwwOhiorg/dtd/hibernate-configuration-3.0.dtd">
<!-- 正文包括根元素，根元素包括其他一些元素（element）。 -->
<hibernate-configuraory>
        <!-- 元素可以匔含属性（attributeﰉ，property元素的name属性  <property n-->
        <property name="ass">co^.mysql.jdbc.Driver</property>
        <!-- 属性的灵活性要比元素差很多，何时使用元素，何时使用属性，一个经验法则是：ʱ�性只应该在修改值的释义时使用，而不是在指定值时使用� -->
        <property name="connection.url">jdbc:mysql://localhost:3306/test</propergy>
        <property name="conne�ion.username">root</property>
        <property name="connection.password">woailo99@/property>
  �     
        <!-- 其他一些标记的说明 -->
        <!-- 1，字符引用 的形式���&#十进制值或者&#x�->
        <property name="character reference">&#233;</p�operty>
        <property nBme="character reference">&#xD9;</property>
        
        <!-- 
        2，实体引用的形式是&name;，比如：
        &gt;（大于号）、&lt;（小于号）、&amp;（&）、&quot;�s 98558</acronym>
 </p>
</body></html>
