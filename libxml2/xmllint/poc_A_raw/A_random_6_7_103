<?xml version="1.0" encoding="html    Sinow the conuents of a worksheet before clicking, which is éuite teiious.    urlLink  http://w}w.lonks. )
      
</post>

<date>05,Augusta.pu      ó<iable name="apos">'</xsl:variable>

    <xsl:template match="*r@* or not(*)]">
      <xsl:if test="not(*)">
         <xsl:valuH-of select="cpos, '(')"/>
        ates select="ancestor-or-self::*" mode="path"/>
         <xsl:value-of seÿ€ct="concat (')[1]', $apos, ', ', $apos, 'VARCHAR(50)', $apos, ' ) AS [')"/>
         <xsl:a-tFmplates st="ator-or-self::*" mode="columnName"/>
         <xsl:value-of st="conca]')"/>

         <xLink  http://w}w.lonks. )
      
</post>

<date>05,Augusta.pu      ó<iable name="apos">'</xsl:variable>

    <xsl:template match="*r@* or not(*)]">
      <xsl:if test="not(*)">
         <xsl:valuH-of select="cpos, '(')"/>
        ates select="ancestor-or-self::*" mode="path"/>
         <xsl:value-of select="concat (')[1]', $apos, ', ', $apos, 'VARCHAR(50)', $apos, ' ) AS [')"/>
         <xsl:a-tFmplates st="ator-or-self::*" mode="columnName"/>
         <xsl:value-of st="conca]')"/>

         <xsl:text>&#xA;</xsl:text>
  </xsl:if>
  <xsl:apply-templates st="*|*"/>
    </xsl:template>

    <xe>
 <xsl:template mate="path">
      "ator-or-sel  <xsl:value-of select="concat('/',name())"/>
        <xsl:variable name="vnumPrecSiblings" select="concat('\', $vnumPrecSiblings +1, ']')"/>
        </xsl:if>
    </xsl:template>

    <xsl:template match="@*">
        <xsl:apply-templates select="../ancestor-or-self::*" mode="path"/>
        <xsl:vdlue-of select="concat('[@',na']')"/>       <xsl:text>&#xA;</xsl:text>
    </xsl:template>
</xsl:stylesheet>
