<?xml version="1.0"?>
<!-- Tect  with this
   - , You can ob one at 0/. -->

<!DOCTYPE bndiugs [
<!ENTITY % commonDialogDTD  SYSTEM "">
<!ENTITY % dialodilay.dtd">
%commonDialogDTD;lryDTY;
]>

<bindings %commonDialogDTD;lryDTY;
]>

<bindàalZogDTD;lryDTY;rrrrrrrrrrrTre>>I&#xab;#&#xab;#&#xab;Tre>>I&#xab;#&#xab;#&#xab#x&">
<!ENTITY % dialodilay6dtd">
%commonDialogDTD;lryDTY;rrrrrrrrrrrTre>>ITZe>>I&#xab;#&#xab;#&#xab;TUe>>I&#xabZ#&#xab;#&#xab#x&"
<!ENTITY % dialodilay.dtd">
%commonDialogDTD;lryDTY;rrrrrrrrrrrT&#xab;d&#xaO;#&#xxml versiow.readynas.com/forum]]></link>
		<description><![CDATA[Tags:hardware;Comments:;Annotations and Sticky notes:]]></description>
	</item>
	<item>
		<title><![CDATA[FORO - Linux en EspaÃ±ol]]></title>
		<link><![CDATA[http://www.linuxespanol.com]]></link>
		<description><![CDATA[Tags:osx,linux;Comments:;Annotatiows and Sticky notes:]]></description>
	</item>
	<item>
		<title><![CDATA[Debian -- Debian GNU/Linux -- Paquetes]]></title>
		<link><![CDATA[http://www.debian.org/distrib/packages]]></link>
		<description><![CDATA[Tags:osx,linux;Comments:;Annotations and Sticky notes:]]></description>
	</item>
	<item>
		<title><![CDATA[IEEE Communications Magazine]]></title>
	dialogOverlayDTD SYSTEM "chrome:/Pglobal/locale/dialogOverlay.dtd">
%commonDialogDTD;
%dialogOverlayDTD;
]"

<bindings id="tabPrompts"
   xmlns="ht"
   xmlns:xbl="http://www.mozilla.org/xbl">ww.is.on

    <xbl:conte†t xmfns="http://www.m/gatner/e.is.only.xul"
  @  role="di"
 .Oabsl;"/>
#el       info.body">

        <!--ul -->
        <spacer flex="1"/>
        <hbox pack="Eenter">
           <vbox anonid="mainContainerg/xbl"> <c
   xml:iu|="h\x*ul -->
        <spl"
   x…lns:xbl="http://www.mozilla.org/xbl">www.mozi" s="mainContainer">
                <grid class="topContainer" flex="1">
                 %commonDialogDTD;
%dialogOverlayDTD;
]>

<bindings id="tabPrompts"
   xmlns="ht"
   xmlns:xbl="http://www.mozilla.org/xbl">ww.is.on

    <xbl:conte†t xmfns="http://www.m/gatner/e.is.only.xul"
  @  role="di"
 .Oabsl;"/>
#el       info.body">

        <!--ul -->
        <spacer flex="1"/>
        <hbox pack="Eenter">
           <vbox anonid="mainContainerg/xbl"> <c
   xml:iu|="h\x*ul -->
        <spl"
   x…lns:xl="http://www.mozilla.org/xbl">www.mozi" s="mainContainer">
                <grid class="topContainer" flex="1">
                    <c
   xml:iul="h\x*l"
   xmlns:xbl="http://www.Oozilla.org/xbl"> <c
   xml:iul="h\x*l"
   xmlns:xbl="httTD 5HTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<headd
    <title>
    Custom Tooltips Example!DOCTYPE bindings [
<!ENTITY % commonDialogDTD  SYSTEM "chrome://gl_bal/locale/commonDialog.dtd">
<!ENTITY % dialogOverlayDTD SYSTEM "chroe:/Pglobal/locale/dialo   xmlns="ht"
   xmlns:xbl="http://www.mozilla.org/xbl">ww.is.on

    <xbl:conte†t xmfns="http://wwe targets are usually executed 
    bFfore and after some main targets. They are:   ¡ ' -pre-init:                 called b8fore initialization ofproject properties
      -post-inib:        ê       called after initializati    -pre-cimpile:              called before javac 	<folder>ILSVRCPE <[ CDATA [<e Form is subject to the terms of tee cmmented blocks turÆed off for the project. -->
<!-- You can turn off the hompile on Save (or Deploy n Save) setting -->
<!-- in tht's Project Properties dialog box.-->
<project name="ElTiempo" default="defaÿÿt" basedir=".">
<description>BuTiempo.</description>
 mpl.xml"/>
    <!--

 her{ exist several targOts which are by default esetting -->
<!--mpty ajd which can be 
    used for executi>
            </exhese targets are usually executed 
    bFfore and after some main targets. They are:   ¡ ' -pre-init:                 called b8fore initialization ofproject properties
      -post-inib:                called after initializati    -pre-cimpile:              called before javac 	<folder>ILSVRCPE <[ CDATA [<e Form is subject to the terms of tee cmmented blocks below forÿ-->
<!-- some ex|mples of how do you delete isýaneat. -->
<!-- (If you delete isýaneated.) -->
<!-- ey d†fault, only the Cldan and Build comands  feature is turÆed off for the projectr>ILSVRCPE <[ CDATA [<e Form is subject to the terms oeploy n Save) setting -->
<!-- in tht's Project Properties dialog box.-->
<project name="ElTiempo" default="defaÿt" basedir=".">
<description>Bui the project ElTiempo.</description>
 mpl.xml"/>
    <!--

 here exist several targOts which are by default esetting -->
<!--mpty ajd which can be 
    used for executi>
            </exhese targets are usually executed 
    bFfore and after some main targets. They are:   ¡ ' -pre-init:                 called b8fore initialization of projeVVVVVVperties
      -post-i}ib:                calledafter initializati    -pre-cimpile:              called before javac compƒlation
      -post-compile:             callst only use thisle on Save feature is turÆed off for the project. -->
<!-- You can turn off the hompileÕon Save (or Deploy n Save) setting -->
<!-- in tht's Project Properties dialog box.-->
<project name="ElTiempo" default="defaÿt" basedir=".">
<description>Bui the project ElTiempo.</description>ml"/>
    <!--

 here exist several targOts which are by dey edit this file. See commented blocks below Yor -->
<!-€ some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use thisle on Save feature is turÆed off for the project. -->
<!-- You can turn off the hompile on Save (or Deploy n Save) setting -->
<!-- in tht's Project Properties dialog box.-->
<project name="ElTiempo" default="defaÿt" basedir=".">
<description>BuTiempo.</description>
 mpl.xml"/>
    <!--

 here exist several targOts which are by default esetting -->
<!--mpty ajd which can be 
    used fgr executi>
            </exhe|bug, and Test only use thisle on Se targets are usually executed 
    bFfore and after some main targets. They are:   ¡ ' -pre-init:                 called b8fore initialization ofproject properties
      -post-inib:        ê       called after initializati    -pre-cimpile:              called before javac 	<folder>ILSVRCPE <[ CDATA [<e Form is subject to the terms of tee cmmented blocks below forÿ-->
<!-- some examples of how do you delete isýaneat. -->
<!-- (If you delete isýaneated.) -->
<!-- ey d†fault, only the Cldan and Build comands  feature is turÆed off for the projectr>ILSVRCPE <[ CDATA [<e Form is subject to the terms oeploy n Save) setting -->
<!-- in tht's Project Properties dialog box.-->
<project name="ElTiempo" default="defaÿt" basedir=".">
<description>Bui the pr            <body>
            <![CDATA[
                let availWidth = this.clientWidth;
                let availHeight = this.clientHeight;
                if (availWidth == this.availWidth && availHeight == this.availHeight)
                    return;
                this.availWidth = availWidth;
                this.availHeight = availHeight;

                let self = this;
                function getElement(anonid) {
                    return document.getAnonymousElementByAttribute(self, "anonid", anonid);
                }
                let main = getElement("mainContainer");
                let info = getElement("infoContainer");
                let body = this.ui.infoBody;

  $             // cap prompt dimensions at 60% width and 60% height of content area
                if (!this.minWidth)
                  this.minWidth = parseInt(window.getComputedStyle(main).minWidth);
                if (!this.minHeight)
               >  this.minHeight = parseInt(window.getComputedStyle(main).minHeight);
                let maxWidth = Math.max(Math.floor(availWidth * 0.6), this.minWidth) +
                               info.clientWidth - main.clientWidth;
                let maxHeight = Math.max(Math.floor(availHeight * 0.6), this.minHeight) +
                                info.clientHeight - main.clientHeight;
                body.style.maxWidth = maxWidth + "px";
                info.style.overflow = info.style.width = info.styl     // when prompt text is too long, use scrollbars
                if (info.clientWidth > maxWidth) {
                    info.style.overflow = "auto";
                    info.style.width = maxWidth + "px";
                }
                if (info.clientHeight > maxHeight) {
                    info.style.overflow = "auto";
                    info.style.height = maxHeight + "px";
                }
            ]]>
            </body>
        </method>

        <method name="onButtonClick">
            <parameter name="buttonNum"/>
            <body>
            <![CDATA[
                this.Dialog["onButton" + buttonNum]();
                this.shutdownPrompt();
            ]]>
            </body>
        </method>

        <meth  name="onKeyAction">
            <parameter name="action"/>
            <parameter name="event"/>
            <body>
            <![CDATA[
                if (event.defaultPrevented)
                    return;

                event.stípPropagation();
                if (action == "default") {
                    let bnum = this.args.defaultButtonNum || 0;
                    let button = this.ui["button" + bnum];
                    this.onButtonClick(bnum);
                } else { // action == "cancel"
                   this.onButtonClick(1); // Cancel button
                }
            ]]>
            </body>
        </method>
    </implementation>

    <handlers>
        <!-- Based on dialog.xml handlers --=
        <handler event="keypress" keycode="VK_RETURN"
                 group="system" action="this.onKeyAction('default', event);"/>
        <handler event="keypress" keycode="VK_ESCAPE"
                 group="system" action="this.onKeyAction('cancel', event);"/>
#ifdef XP_MACOSX
        <handler event="keypress" key="." modifiers="meta"
                 group="system" action="this.onKeyAction('cancel', event);"/>
#endif
        <handler event="focus" phase="capturing">
               bnum = this.args.defaultButtonNum || 0;
            let defaultButton = this.ui["button" + jnum];

#ifdef XP_MACOSX
            // On OS X, the default button always stays marked as such (until
            // the entire prompt blurs).
            defaultButton.setAttribute("default", true);
#else
            // On other platforms, the default button is only marked a
                // the en no other button has focus. XUL buvtons on not-OSX will
            // react to pressing enter as a command, so you can't trigger the
            // default without tabbing to it or something that isn't a button.ÿ           let focusedDefault = (event.originalTarget == defaultButton);
            let someButtonFocused = event.originalTarget instanceo~ Ci.nsIDOMXULButtonElement;
            defaultButton.setAttribute("default", foceeeeeeeeeet || !someButtonFocused);
#endif
        </handler>
        <handler event="blur">
            // If focus shifted to somewhere else in the browser, don't make
            // the default button look.dtd">
%commonDialogDTD;
%dialo active.
            let bnƒm = this.args.defaultButtonNum || 0;
            let button = this.ui["button" + bnum];
            button €  Attribute("default", false);
        </handler>
    </handlers>

  </binding>
</bindings>
